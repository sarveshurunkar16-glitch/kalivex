<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.kalivex.app"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Post notifications permission for Android 13+; will be requested at runtime where needed -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:label="KALIVEX"
        android:usesCleartextTraffic="false">

        <service android:name="com.kalivex.app.voice.KalivexVoiceService"
            android:foregroundServiceType="microphone|connectedDevice"
            android:exported="false" />

        <activity android:name="com.kalivex.app.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
